/**
 * For a basic, out-of-browser application (running e.g. on Node.js, Rhino).
 */
//>public
vjo.ctype('qx.application.Basic')
.inherits('qx.core.Object')
.satisfies('qx.application.IApplication')
.protos({

})
.endType();