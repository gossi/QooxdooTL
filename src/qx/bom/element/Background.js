/**
 * The background class contains methods to compute and set the background image
 * of a DOM element.
 * 
 * It fixes a background position issue in Firefox 2.
 */
//>public
vjo.ctype('qx.bom.element.Background')
.endType();