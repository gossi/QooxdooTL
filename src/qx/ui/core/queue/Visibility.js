/**
 * Keeps data about the visibility of all widgets. Updates the internal
 * tree when widgets are added, removed or modify their visibility.
 */
//>public
vjo.ctype('qx.ui.core.queue.Visibility')
.endType();